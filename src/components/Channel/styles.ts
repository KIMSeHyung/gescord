import styled from "../../styles/themed-components";

export const ChannelsLeftTop = styled.div`
  height: 50px;
  border-bottom: 1px solid ${({ theme }) => theme.color.baseBlack};
  display: flex;
  justify-content: center;
  align-items: center;
`;
